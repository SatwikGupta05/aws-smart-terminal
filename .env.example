# AWS Authentication Method
# Options: 'iam', 'root', or 'demo'
# - 'iam': Use IAM user credentials (recommended)
# - 'root': Use root account credentials (not recommended)
# - 'demo': No AWS login - AI suggestions only (no execution)
AWS_AUTH_METHOD=root

# AWS Credentials (IAM User - Recommended)
AWS_ACCESS_KEY_ID=your_iam_access_key_here
AWS_SECRET_ACCESS_KEY=your_iam_secret_key_here
AWS_DEFAULT_REGION=us-east-1

# AWS Root Account Credentials (NOT RECOMMENDED - Use only if absolutely necessary)
# Only used when AWS_AUTH_METHOD=root
AWS_ROOT_ACCESS_KEY_ID="Access key here"
AWS_ROOT_SECRET_ACCESS_KEY="Access key here"

# Gemini API
GEMINI_API_KEY="API key here"

# Terminal Settings
HISTORY_FILE=.terminal_history
MAX_HISTORY_ENTRIES=1000
